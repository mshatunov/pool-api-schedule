spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/pool

feign:
  hystrix:
    enabled: true

common:
  params:
    duration: 30
    time-open: "10:00"
    time-close: "21:00"
    default-depth: 30